<!DOCTYPE html>
<html>
<head>
    <title>Library - Borrowed Books</title>
    <link rel="stylesheet" href="/static/tailwind.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-6 text-purple-700">Borrowed Books</h1>
        <div class="mb-4 flex gap-4">
            <a href="/" class="text-blue-600 hover:underline">Books</a>
            <a href="/members" class="text-blue-600 hover:underline">Members</a>
        </div>
        <div class="overflow-x-auto">
        <table class="min-w-full bg-white shadow rounded-lg">
            <thead class="bg-purple-100">
                <tr>
                    <th class="py-2 px-4">Book ID</th>
                    <th class="py-2 px-4">Title</th>
                    <th class="py-2 px-4">Member</th>
                    <th class="py-2 px-4">Issue Date</th>
                    <th class="py-2 px-4">Due Date</th>
                </tr>
            </thead>
            <tbody>
                {{range .}}
                <tr class="border-b hover:bg-purple-50">
                    <td class="py-2 px-4">{{.BookID}}</td>
                    <td class="py-2 px-4 font-semibold">{{.Title}}</td>
                    <td class="py-2 px-4">{{.Member}}</td>
                    <td class="py-2 px-4">{{.IssueDate}}</td>
                    <td class="py-2 px-4">{{.DueDate}}</td>
                </tr>
                {{end}}
            </tbody>
        </table>
        </div>
    </div>
</body>
</html>
