<!DOCTYPE html>
<html>
<head>
    <title>Book Details</title>
    <link rel="stylesheet" href="/static/pico.min.css">
    <style>
        .book-info-table th {
            text-align: right;
            {{/* color: #666666; */}}
            font-weight: 500;
            width: 40%;
        }
        .book-info-table td {
            text-align: left;
            font-weight: 400;
        }
        {{/* .action-card {
            padding: 1.2em 1.5em;
            border-radius: 10px;
            box-shadow: 0 2px 8px #0001;
            background: transparent;
            margin-bottom: 1em;
        } */}}
        .action-card h2 {
            margin-top: 0;
        }
        {{/* .delete-card {
            border: 1px solid #fbb;
            background: transparent;
        }
        .delete-card h2, .delete-card p {
            color: #c00;
        } */}}
        .main-flex {
            display: flex;
            gap: 2em;
            align-items: flex-start;
            flex-wrap: wrap;
        }
        @media (max-width: 900px) {
            .main-flex { flex-direction: column; }
        }
    </style>
</head>
<body>
    <main class="container">
        <nav>
            <ul>
                <li><a href="/">Books</a></li>
                <li><a href="/members">Members</a></li>
                <li><a href="/borrowed">Borrowed Books</a></li>
                <li><a href="/isbn-lookup">ISBN Lookup</a></li>
                <li><a href="/reports">Reports</a></li>
            </ul>
        </nav>
        <a href="/">&larr; Back to Books</a>
        <h1 style="margin-bottom:0.2em;">{{.Book.Title}}</h1>
        <div class="main-flex" style="gap:2em;">
            <section style="flex:1 1 0; width: 40%;">
                <article class="action-card">
                    <h2 style="margin-top:0;">Book Information</h2>
                    <table class="book-info-table" style="width:100%;margin-bottom:0.5em;">
                        <tr><th>Author</th><td>{{.Book.Author}}</td></tr>
                        <tr><th>ISBN</th><td>{{.Book.ISBN}}</td></tr>
                        <tr><th>Publication Year</th><td>{{.Book.PublicationYear}}</td></tr>
                        <tr><th>Copies Available</th><td>{{.Book.CopiesAvailable}} / {{.Book.CopiesTotal}}</td></tr>
                    </table>
                {{/* </article>
                <article class="action-card delete-card"> */}}
                    {{/* <h2>Delete Book</h2> */}}
                    {{/* <p style="font-size:0.95em;">This action cannot be undone.</p> */}}
                    <form method="POST" action="/delete-book" >
                        <input type="hidden" name="book_id" value="{{.Book.ID}}">
                        <button type="submit" style="background:#c00;color:#fff;border-color:#D93526">Delete Book</button>
                    </form>
                </article>

                <article class="action-card">
                    <h2>Borrow Book</h2>
                    <form method="POST" action="/borrow" style="display:grid; gap:0.7em;">
                        <input type="hidden" name="book_id" value="{{.Book.ID}}">
                        <label>Member
                            <select name="member_id" required>
                                <option value="">Select member</option>
                                {{range .Members}}
                                <option value="{{.ID}}">{{.Name}} ({{.MemberID}})</option>
                                {{end}}
                            </select>
                        </label>
                        <label>Days
                            <input type="number" name="days" min="1" max="60" value="14" required>
                        </label>
                        <button type="submit">Borrow</button>
                    </form>
                </article>
            </section>
            <section style="flex:1 1 0; width: 40%; display: flex; flex-direction: column; gap: 2em;">
                <article class="action-card">
                    <h2>Edit Book</h2>
                    <form method="POST" action="/update-book" style="display:grid; gap:0.7em;">
                        <input type="hidden" name="book_id" value="{{.Book.ID}}">
                        <label>Title
                            <input type="text" name="title" value="{{.Book.Title}}" required>
                        </label>
                        <label>Author
                            <input type="text" name="author" value="{{.Book.Author}}" required>
                        </label>
                        <label>ISBN
                            <input type="text" name="isbn" value="{{.Book.ISBN}}" readonly required>
                        </label>
                        <label>Publication Year
                            <input type="number" name="publication_year" value="{{.Book.PublicationYear}}" required>
                        </label>
                        <label>Copies Total
                            <input type="number" name="copies_total" min="1" value="{{.Book.CopiesTotal}}" required>
                        </label>
                        <button type="submit">Update Book</button>
                    </form>
                </article>

            </section>
        </div>
    </main>
</body>
</html>
