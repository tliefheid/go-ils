<!DOCTYPE html>
<html>
<head>
    <title>Library - Books</title>
    <link rel="stylesheet" href="/static/tailwind.min.css">
</head>
<body class="bg-gray-50 min-h-screen">
    <div class="container mx-auto py-8">
        <h1 class="text-3xl font-bold mb-6 text-blue-700">Books</h1>
        <div class="mb-4 flex gap-4">
            <a href="/members" class="text-blue-600 hover:underline">Members</a>
            <a href="/borrowed" class="text-blue-600 hover:underline">Borrowed Books</a>
        </div>
        <div class="overflow-x-auto">
        <table class="min-w-full bg-white shadow rounded-lg">
            <thead class="bg-blue-100">
                <tr>
                    <th class="py-2 px-4">ID</th>
                    <th class="py-2 px-4">Title</th>
                    <th class="py-2 px-4">Author</th>
                    <th class="py-2 px-4">ISBN</th>
                    <th class="py-2 px-4">Genre</th>
                    <th class="py-2 px-4">Year</th>
                    <th class="py-2 px-4">Available</th>
                    <th class="py-2 px-4">Total</th>
                    <th class="py-2 px-4">Details</th>
                </tr>
            </thead>
            <tbody>
                {{range .}}
                <tr class="border-b hover:bg-blue-50">
                    <td class="py-2 px-4">{{.ID}}</td>
                    <td class="py-2 px-4 font-semibold">{{.Title}}</td>
                    <td class="py-2 px-4">{{.Author}}</td>
                    <td class="py-2 px-4">{{.ISBN}}</td>
                    <td class="py-2 px-4">{{.Genre}}</td>
                    <td class="py-2 px-4">{{.PublicationYear}}</td>
                    <td class="py-2 px-4">{{.CopiesAvailable}}</td>
                    <td class="py-2 px-4">{{.CopiesTotal}}</td>
                    <td class="py-2 px-4"><a href="/book?id={{.ID}}" class="text-blue-500 hover:underline">View</a></td>
                </tr>
                {{end}}
            </tbody>
        </table>
        </div>
    </div>
</body>
</html>
